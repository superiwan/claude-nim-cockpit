# Claude Code 通过 LiteLLM 接入其他模型

这份文档整理的是一套已经跑通的方案：让 Claude Code 看起来仍然在走 `Anthropic` 接口，但实际上把请求转发到本机 `LiteLLM`，再由 `LiteLLM` 转到其他模型提供方。

当前这套现成实现使用的是：

- Claude Code / VSCode Claude
- `ANTHROPIC_BASE_URL=http://127.0.0.1:4000`
- 本机 `LiteLLM` 网关
- NVIDIA NIM 模型

如果你想复用这套思路去接 Kimi、GLM、MiniMax、Step 等模型，这份 README 可以直接照着部署。

## 1. 整体架构

```text
Claude Code / VSCode Claude
-> ANTHROPIC_BASE_URL=http://127.0.0.1:4000
-> LiteLLM（本机网关）
-> 真实模型提供方（这里以 NVIDIA NIM 为例）
```

核心思路只有两点：

1. 把 Claude Code 的 `ANTHROPIC_BASE_URL` 改到本机 LiteLLM。
2. 在 LiteLLM 里把 Claude 风格的模型名映射到真实模型。

## 2. 部署方案选择

### 方案 A：本机 LiteLLM 网关转发

优点：

- Claude Code 基本不需要魔改，兼容性最好
- 可以自己定义模型别名，便于在 Claude 的 `sonnet / opus / haiku` 逻辑上做映射
- 后续切模型只需要改 LiteLLM 配置或环境变量
- 可控性强，排查问题方便

缺点：

- 需要本机常驻一个 LiteLLM 进程
- 首次部署比直接填第三方地址稍微多几步

### 方案 B：直接把 `ANTHROPIC_BASE_URL` 指向第三方代理

优点：

- 搭建最快
- 少一个本地进程

缺点：

- 可控性差
- 模型别名映射能力弱
- 出问题时不容易判断是 Claude、代理站还是模型侧的问题
- 后续切换模型和迁移方案不如 LiteLLM 清晰

### 推荐方案

推荐使用方案 A，也就是“本机 LiteLLM 网关转发”。

原因很简单：你现在这套已经验证过能跑，而且最适合长期维护。后面无论你是换提供方、补模型映射、还是给别人复刻部署，这种方式都最稳。

## 3. 前置条件

本文默认你在 Windows 环境下部署。

你需要准备：

- Claude Code 或 VSCode Claude
- 一个可用的 Python 环境
- 能在终端里直接运行的 `litellm` 命令
- 目标模型提供方的 API Key
- PowerShell

建议准备：

- VSCode，并确保命令行里能使用 `code`
- 把下面的脚本统一放到 `%USERPROFILE%\.claude\`

## 4. 目录约定

建议使用下面这套目录布局：

```text
%USERPROFILE%\.claude\
├─ litellm.config.yaml
├─ NIM_MODEL_MAINTENANCE.md
└─ nim-bridge\
   ├─ enable_nim_global.ps1
   ├─ disable_nim_global.ps1
   ├─ start_litellm.ps1
   ├─ stop_litellm.ps1
   ├─ switch-claude-model.ps1
   ├─ verify_gateway.ps1
   └─ start-vscode-with-nim.ps1
```

本文后面的示例都按这个布局来写。

## 5. 安装 LiteLLM

只要目标是让终端里能运行 `litellm` 命令即可。

如果你使用的是普通 Python 环境，可以先试：

```powershell
pip install "litellm[proxy]"
```

如果你已经有自己的 Conda / venv 环境，也可以装在你常用的那个环境里。安装完成后请先确认：

```powershell
litellm --help
```

如果这条命令能正常执行，说明 `litellm` 已经可用了。

官方文档入口可参考：[LiteLLM Getting Started](https://docs.litellm.ai/)

## 6. 写 LiteLLM 配置

在 `%USERPROFILE%\.claude\litellm.config.yaml` 中写入如下内容：

```yaml
model_list:
  - model_name: sonnet-glm5
    litellm_params:
      model: nvidia_nim/z-ai/glm5
      api_key: os.environ/NVIDIA_NIM_API_KEY
      api_base: os.environ/NVIDIA_NIM_API_BASE
      timeout: 120

  - model_name: opus-minimax
    litellm_params:
      model: nvidia_nim/minimaxai/minimax-m2.5
      api_key: os.environ/NVIDIA_NIM_API_KEY
      api_base: os.environ/NVIDIA_NIM_API_BASE
      timeout: 120

  - model_name: haiku-kimi
    litellm_params:
      model: nvidia_nim/moonshotai/kimi-k2.5
      api_key: os.environ/NVIDIA_NIM_API_KEY
      api_base: os.environ/NVIDIA_NIM_API_BASE
      timeout: 120

  - model_name: kimi-k2.5
    litellm_params:
      model: nvidia_nim/moonshotai/kimi-k2.5
      api_key: os.environ/NVIDIA_NIM_API_KEY
      api_base: os.environ/NVIDIA_NIM_API_BASE
      timeout: 120

  - model_name: glm5
    litellm_params:
      model: nvidia_nim/z-ai/glm5
      api_key: os.environ/NVIDIA_NIM_API_KEY
      api_base: os.environ/NVIDIA_NIM_API_BASE
      timeout: 120

  - model_name: minimax-m2.5
    litellm_params:
      model: nvidia_nim/minimaxai/minimax-m2.5
      api_key: os.environ/NVIDIA_NIM_API_KEY
      api_base: os.environ/NVIDIA_NIM_API_BASE
      timeout: 120

  - model_name: step-3.5-flash
    litellm_params:
      model: nvidia_nim/stepfun-ai/step-3.5-flash
      api_key: os.environ/NVIDIA_NIM_API_KEY
      api_base: os.environ/NVIDIA_NIM_API_BASE
      timeout: 120

general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY
  disable_spend_logs: true
  store_model_in_db: false

litellm_settings:
  drop_params: true
```

### 这份配置的作用

- `sonnet-glm5`、`opus-minimax`、`haiku-kimi` 是给 Claude 风格的三档别名准备的
- `glm5`、`minimax-m2.5`、`kimi-k2.5`、`step-3.5-flash` 是直接模型名
- `drop_params: true` 很重要，可以避免某些不被下游模型支持的参数把请求打挂

## 7. 初始化脚本

下面这组脚本是这套方案最实用的部分。建议原样建立。

### 7.1 `start_litellm.ps1`

路径：`%USERPROFILE%\.claude\nim-bridge\start_litellm.ps1`

```powershell
param(
  [string]$ConfigPath = "$env:USERPROFILE\.claude\litellm.config.yaml",
  [int]$Port = 4000
)

$ErrorActionPreference = "Stop"

if (-not (Get-Command litellm -ErrorAction SilentlyContinue)) {
  throw "未找到 litellm 命令，请先确认已安装并可在 PATH 中使用。"
}

$existing = Get-CimInstance Win32_Process |
  Where-Object { $_.CommandLine -match "litellm" -and $_.CommandLine -match [regex]::Escape($ConfigPath) }

if ($existing) {
  Write-Host "LiteLLM 已在运行（同一配置）。"
  exit 0
}

$logDir = "$env:USERPROFILE\.claude\nim-bridge\logs"
New-Item -ItemType Directory -Force -Path $logDir | Out-Null
$stdout = Join-Path $logDir "litellm.stdout.log"
$stderr = Join-Path $logDir "litellm.stderr.log"

$args = @(
  "--config", $ConfigPath,
  "--host", "127.0.0.1",
  "--port", "$Port"
)

Start-Process -FilePath "litellm" -ArgumentList $args -WindowStyle Hidden -RedirectStandardOutput $stdout -RedirectStandardError $stderr | Out-Null
Start-Sleep -Seconds 2

$masterKey = [Environment]::GetEnvironmentVariable("LITELLM_MASTER_KEY", "User")
if ([string]::IsNullOrWhiteSpace($masterKey)) {
  $masterKey = $env:LITELLM_MASTER_KEY
}

$headers = @{}
if (-not [string]::IsNullOrWhiteSpace($masterKey)) {
  $headers["Authorization"] = "Bearer $masterKey"
}

$ok = $false
for ($i = 0; $i -lt 10; $i++) {
  try {
    $models = Invoke-RestMethod -Method Get -Uri "http://127.0.0.1:$Port/v1/models" -Headers $headers -TimeoutSec 5
    $ok = $true
    break
  } catch {
    Start-Sleep -Seconds 2
  }
}

if (-not $ok) {
  throw "LiteLLM 端口 $Port 在等待窗口内未就绪。"
}

$count = 0
if ($models -and $models.data) {
  $count = @($models.data).Count
}

Write-Host "LiteLLM 启动成功。可见模型数: $count"
```

### 7.2 `stop_litellm.ps1`

路径：`%USERPROFILE%\.claude\nim-bridge\stop_litellm.ps1`

```powershell
param(
  [string]$ConfigPath = "$env:USERPROFILE\.claude\litellm.config.yaml"
)

$ErrorActionPreference = "Stop"

$targets = Get-CimInstance Win32_Process |
  Where-Object {
    ($_.Name -match "litellm|python") -and
    $_.CommandLine -match "litellm" -and
    $_.CommandLine -match [regex]::Escape($ConfigPath)
  }

if (-not $targets) {
  Write-Host "未找到匹配该配置的 LiteLLM 进程。"
  exit 0
}

foreach ($p in $targets) {
  Stop-Process -Id $p.ProcessId -Force
  Write-Host ("已停止 LiteLLM 进程 PID={0}" -f $p.ProcessId)
}
```

### 7.3 `enable_nim_global.ps1`

路径：`%USERPROFILE%\.claude\nim-bridge\enable_nim_global.ps1`

```powershell
param(
  [Parameter(Mandatory = $true)]
  [string]$NvidiaApiKey,
  [string]$NvidiaApiBase = "https://integrate.api.nvidia.com/v1",
  [string]$ModelName = "kimi-k2.5",
  [string]$ConfigPath = "$env:USERPROFILE\.claude\litellm.config.yaml",
  [string]$BackupDir = "$env:USERPROFILE\.claude\nim-bridge\backups"
)

$ErrorActionPreference = "Stop"

New-Item -ItemType Directory -Force -Path $BackupDir | Out-Null

$trackedVars = @(
  "NVIDIA_NIM_API_KEY",
  "NVIDIA_NIM_API_BASE",
  "LITELLM_MASTER_KEY",
  "ANTHROPIC_BASE_URL",
  "ANTHROPIC_API_KEY",
  "ANTHROPIC_MODEL",
  "CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS"
)

$backup = [ordered]@{
  created_at = (Get-Date).ToString("o")
  vars = @{}
}

foreach ($name in $trackedVars) {
  $backup.vars[$name] = [Environment]::GetEnvironmentVariable($name, "User")
}

$stamp = Get-Date -Format "yyyyMMdd_HHmmss"
$backupFile = Join-Path $BackupDir "env_backup_$stamp.json"
$backup | ConvertTo-Json -Depth 5 | Set-Content -Path $backupFile -Encoding UTF8

$masterKey = [Environment]::GetEnvironmentVariable("LITELLM_MASTER_KEY", "User")
if ([string]::IsNullOrWhiteSpace($masterKey)) {
  $random = -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 40 | ForEach-Object { [char]$_ })
  $masterKey = "sk-local-$random"
}

[Environment]::SetEnvironmentVariable("NVIDIA_NIM_API_KEY", $NvidiaApiKey, "User")
[Environment]::SetEnvironmentVariable("NVIDIA_NIM_API_BASE", $NvidiaApiBase, "User")
[Environment]::SetEnvironmentVariable("LITELLM_MASTER_KEY", $masterKey, "User")
[Environment]::SetEnvironmentVariable("ANTHROPIC_BASE_URL", "http://127.0.0.1:4000", "User")
[Environment]::SetEnvironmentVariable("ANTHROPIC_API_KEY", $masterKey, "User")
[Environment]::SetEnvironmentVariable("ANTHROPIC_MODEL", $ModelName, "User")
[Environment]::SetEnvironmentVariable("CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS", "1", "User")

$env:NVIDIA_NIM_API_KEY = $NvidiaApiKey
$env:NVIDIA_NIM_API_BASE = $NvidiaApiBase
$env:LITELLM_MASTER_KEY = $masterKey
$env:ANTHROPIC_BASE_URL = "http://127.0.0.1:4000"
$env:ANTHROPIC_API_KEY = $masterKey
$env:ANTHROPIC_MODEL = $ModelName
$env:CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS = "1"

& "$env:USERPROFILE\.claude\nim-bridge\start_litellm.ps1" -ConfigPath $ConfigPath -Port 4000

$headers = @{
  "Authorization" = "Bearer $masterKey"
  "Content-Type" = "application/json"
}

$models = Invoke-RestMethod -Method Get -Uri "http://127.0.0.1:4000/v1/models" -Headers $headers -TimeoutSec 12
$hasModel = $false
if ($models -and $models.data) {
  $hasModel = @($models.data.id) -contains $ModelName
}

if (-not $hasModel) {
  Write-Warning "LiteLLM 已启动，但 /v1/models 中未看到 $ModelName，请检查映射。"
} else {
  Write-Host "已完成全局切换。模型 $ModelName 可见。"
}

Write-Host "备份文件: $backupFile"
Write-Host "提示: 需要新开终端后，Claude Code 才会读取新的用户级环境变量。"
```

### 7.4 `switch-claude-model.ps1`

路径：`%USERPROFILE%\.claude\nim-bridge\switch-claude-model.ps1`

```powershell
param(
  [ValidateSet("kimi-k2.5", "glm5", "minimax-m2.5", "step-3.5-flash", "sonnet-glm5", "opus-minimax", "haiku-kimi")]
  [string]$ModelName,
  [string]$Workspace = "",
  [switch]$RestartVSCode,
  [switch]$NoOpen
)

$ErrorActionPreference = "Stop"

if ([string]::IsNullOrWhiteSpace($ModelName)) {
  throw "请传入 -ModelName。"
}

$bridgeRoot = "$env:USERPROFILE\.claude\nim-bridge"
$startScript = Join-Path $bridgeRoot "start_litellm.ps1"

if (-not (Test-Path $startScript)) {
  throw "未找到启动脚本：$startScript"
}

& $startScript

[Environment]::SetEnvironmentVariable("ANTHROPIC_MODEL", $ModelName, "User")

$env:ANTHROPIC_MODEL = $ModelName
$env:ANTHROPIC_BASE_URL = [Environment]::GetEnvironmentVariable("ANTHROPIC_BASE_URL", "User")
$env:ANTHROPIC_API_KEY = [Environment]::GetEnvironmentVariable("ANTHROPIC_API_KEY", "User")
$env:CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS = [Environment]::GetEnvironmentVariable("CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS", "User")

Write-Host "已切换 Claude 默认模型: $ModelName"

if ($NoOpen) {
  exit 0
}

if (-not (Get-Command code -ErrorAction SilentlyContinue)) {
  throw "未找到 VSCode 命令 code。"
}

if ($RestartVSCode) {
  Get-Process -Name Code -ErrorAction SilentlyContinue | Stop-Process -Force
  Start-Sleep -Seconds 1
}

if ([string]::IsNullOrWhiteSpace($Workspace)) {
  $Workspace = $env:USERPROFILE
}

Start-Process -FilePath "code" -ArgumentList @("-n", $Workspace) | Out-Null
Write-Host "已打开 VSCode: $Workspace"
```

### 7.5 `verify_gateway.ps1`

路径：`%USERPROFILE%\.claude\nim-bridge\verify_gateway.ps1`

```powershell
param(
  [string]$ModelName = "kimi-k2.5"
)

$ErrorActionPreference = "Stop"

$masterKey = [Environment]::GetEnvironmentVariable("LITELLM_MASTER_KEY", "User")
if ([string]::IsNullOrWhiteSpace($masterKey)) {
  throw "未找到用户级 LITELLM_MASTER_KEY，请先运行 enable_nim_global.ps1"
}

$headers = @{
  "Authorization" = "Bearer $masterKey"
  "Content-Type" = "application/json"
}

$body = @{
  model = $ModelName
  messages = @(
    @{
      role = "user"
      content = "请只回复：NIM_OK"
    }
  )
  max_tokens = 32
  temperature = 0.2
  extra_body = @{
    chat_template_kwargs = @{
      thinking = $false
    }
  }
} | ConvertTo-Json -Depth 6

$resp = Invoke-RestMethod -Method Post -Uri "http://127.0.0.1:4000/v1/chat/completions" -Headers $headers -Body $body -TimeoutSec 60
$msg = $resp.choices[0].message
$text = $msg.content
if ([string]::IsNullOrWhiteSpace($text) -and $msg.reasoning_content) {
  $text = $msg.reasoning_content
}

Write-Host "网关验证返回: $text"
```

### 7.6 `start-vscode-with-nim.ps1`

路径：`%USERPROFILE%\.claude\nim-bridge\start-vscode-with-nim.ps1`

```powershell
param(
  [string]$Workspace = "$env:USERPROFILE",
  [string]$ModelName = "",
  [switch]$NoOpen
)

$ErrorActionPreference = "Stop"

if (-not (Get-Command code -ErrorAction SilentlyContinue)) {
  throw "未找到 VSCode 命令 code，请先在 VSCode 中启用 shell command。"
}

$startScript = "$env:USERPROFILE\.claude\nim-bridge\start_litellm.ps1"
if (-not (Test-Path $startScript)) {
  throw "未找到启动脚本: $startScript"
}

& $startScript

$env:ANTHROPIC_BASE_URL = [Environment]::GetEnvironmentVariable("ANTHROPIC_BASE_URL", "User")
$env:ANTHROPIC_API_KEY = [Environment]::GetEnvironmentVariable("ANTHROPIC_API_KEY", "User")
$env:ANTHROPIC_MODEL = [Environment]::GetEnvironmentVariable("ANTHROPIC_MODEL", "User")
$env:CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS = [Environment]::GetEnvironmentVariable("CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS", "User")

if (-not [string]::IsNullOrWhiteSpace($ModelName)) {
  $env:ANTHROPIC_MODEL = $ModelName
}

if ([string]::IsNullOrWhiteSpace($env:ANTHROPIC_BASE_URL) -or [string]::IsNullOrWhiteSpace($env:ANTHROPIC_API_KEY)) {
  throw "ANTHROPIC_BASE_URL 或 ANTHROPIC_API_KEY 为空，请先运行 enable_nim_global.ps1"
}

Write-Host "NIM 网关已准备就绪。"
Write-Host "BASE_URL: $($env:ANTHROPIC_BASE_URL)"
Write-Host "MODEL: $($env:ANTHROPIC_MODEL)"
Write-Host "Workspace: $Workspace"

if (-not $NoOpen) {
  code $Workspace
}
```

### 7.7 `disable_nim_global.ps1`

路径：`%USERPROFILE%\.claude\nim-bridge\disable_nim_global.ps1`

```powershell
param(
  [string]$BackupDir = "$env:USERPROFILE\.claude\nim-bridge\backups",
  [string]$ConfigPath = "$env:USERPROFILE\.claude\litellm.config.yaml"
)

$ErrorActionPreference = "Stop"

& "$env:USERPROFILE\.claude\nim-bridge\stop_litellm.ps1" -ConfigPath $ConfigPath

$latest = Get-ChildItem -Path $BackupDir -Filter "env_backup_*.json" -ErrorAction SilentlyContinue |
  Sort-Object LastWriteTime -Descending |
  Select-Object -First 1

if (-not $latest) {
  Write-Warning "未找到备份文件，无法自动回滚环境变量。"
  exit 0
}

$backup = Get-Content -Path $latest.FullName -Raw | ConvertFrom-Json
$vars = $backup.vars.PSObject.Properties.Name

foreach ($name in $vars) {
  $value = $backup.vars.$name
  if ($null -eq $value -or $value -eq "") {
    [Environment]::SetEnvironmentVariable($name, $null, "User")
  } else {
    [Environment]::SetEnvironmentVariable($name, [string]$value, "User")
  }
}

Write-Host "已按备份回滚用户级环境变量：$($latest.FullName)"
Write-Host "提示: 需要新开终端后生效。"
```

## 8. 首次启用

准备好上面的配置和脚本后，执行：

```powershell
powershell -ExecutionPolicy Bypass -File "$env:USERPROFILE\.claude\nim-bridge\enable_nim_global.ps1" -NvidiaApiKey "你的真实 API Key" -ModelName "kimi-k2.5"
```

首次执行会做这些事：

- 备份当前用户级环境变量
- 生成或复用 `LITELLM_MASTER_KEY`
- 写入 `ANTHROPIC_BASE_URL`
- 写入 `ANTHROPIC_API_KEY`
- 写入 `ANTHROPIC_MODEL`
- 启动本机 LiteLLM
- 检查 `/v1/models` 里是否能看到目标模型

## 9. 验证部署是否成功

### 9.1 检查环境变量

```powershell
Get-ChildItem Env: | Where-Object { $_.Name -match "ANTHROPIC|LITELLM|NVIDIA_NIM" } | Sort-Object Name
```

至少应能看到：

- `ANTHROPIC_BASE_URL=http://127.0.0.1:4000`
- `ANTHROPIC_API_KEY=<本地 LiteLLM master key>`
- `ANTHROPIC_MODEL=<当前默认模型>`
- `LITELLM_MASTER_KEY=<本地 LiteLLM master key>`
- `NVIDIA_NIM_API_BASE=https://integrate.api.nvidia.com/v1`

### 9.2 检查 LiteLLM 模型列表

```powershell
$mk=[Environment]::GetEnvironmentVariable("LITELLM_MASTER_KEY","User")
(Invoke-RestMethod -Method Get -Uri "http://127.0.0.1:4000/v1/models" -Headers @{Authorization="Bearer $mk"}).data.id
```

应该至少能看到：

- `sonnet-glm5`
- `opus-minimax`
- `haiku-kimi`
- `kimi-k2.5`
- `glm5`
- `minimax-m2.5`
- `step-3.5-flash`

### 9.3 真正发一次请求

```powershell
powershell -ExecutionPolicy Bypass -File "$env:USERPROFILE\.claude\nim-bridge\verify_gateway.ps1" -ModelName "kimi-k2.5"
```

正常时应返回类似：

```text
网关验证返回: NIM_OK
```

## 10. 日常使用

### 切默认模型

```powershell
powershell -ExecutionPolicy Bypass -File "$env:USERPROFILE\.claude\nim-bridge\switch-claude-model.ps1" -ModelName "glm5" -RestartVSCode
```

可切换的模型名取决于你的 `litellm.config.yaml`。

当前示例中可选：

- `kimi-k2.5`
- `glm5`
- `minimax-m2.5`
- `step-3.5-flash`
- `sonnet-glm5`
- `opus-minimax`
- `haiku-kimi`

### 启动带网关环境的 VSCode

```powershell
powershell -ExecutionPolicy Bypass -File "$env:USERPROFILE\.claude\nim-bridge\start-vscode-with-nim.ps1" -Workspace "D:\your_project"
```

### 关闭这套接管

```powershell
powershell -ExecutionPolicy Bypass -File "$env:USERPROFILE\.claude\nim-bridge\disable_nim_global.ps1"
```

### 启用三账号池

现在这套脚本已经支持 3 个 NVIDIA key 作为同一模型组下的 3 个 deployment 使用。

对应环境变量是：

- `NVIDIA_NIM_API_KEY_1`
- `NVIDIA_NIM_API_KEY_2`
- `NVIDIA_NIM_API_KEY_3`

当前 LiteLLM 配置采用的是加权分流：

- `KEY_1` 权重 `6`
- `KEY_2` 权重 `3`
- `KEY_3` 权重 `1`

如果你暂时只传第一个 key，脚本会自动把第二、第三个 key 先回退成第一个，保证兼容旧用法。

如果你要正式启用三账号池，执行：

```powershell
powershell -ExecutionPolicy Bypass -File "$env:USERPROFILE\.claude\nim-bridge\enable_nim_global.ps1" `
  -NvidiaApiKey  "你的账号1 key" `
  -NvidiaApiKey2 "你的账号2 key" `
  -NvidiaApiKey3 "你的账号3 key" `
  -ModelName "kimi-k2.5"
```

如果你只想先保持旧模式，也可以继续只传一个：

```powershell
powershell -ExecutionPolicy Bypass -File "$env:USERPROFILE\.claude\nim-bridge\enable_nim_global.ps1" `
  -NvidiaApiKey "你的账号1 key" `
  -ModelName "kimi-k2.5"
```

## 11. Claude 下拉模型和真实模型的关系

如果你想让 Claude 的 `sonnet / opus / haiku` 风格选择也能工作，就必须在 LiteLLM 里保留这些“桥接别名”。

推荐保持下面这组映射：

| Claude 侧别名 | 实际模型 |
| --- | --- |
| `sonnet-glm5` | `nvidia_nim/z-ai/glm5` |
| `opus-minimax` | `nvidia_nim/minimaxai/minimax-m2.5` |
| `haiku-kimi` | `nvidia_nim/moonshotai/kimi-k2.5` |

这样做的好处是：

- Claude 那边仍然能按熟悉的档位理解
- 你这边可以自由替换真实模型
- 后面换供应商时，Claude 侧不用跟着大改

## 12. 后续新增或替换模型

标准流程如下：

1. 修改 `%USERPROFILE%\.claude\litellm.config.yaml`
2. 在 `model_list` 中新增模型映射
3. 重启 LiteLLM
4. 查询 `/v1/models`
5. 关闭并重开 Claude / VSCode 会话

重启命令：

```powershell
powershell -ExecutionPolicy Bypass -File "$env:USERPROFILE\.claude\nim-bridge\stop_litellm.ps1"
powershell -ExecutionPolicy Bypass -File "$env:USERPROFILE\.claude\nim-bridge\start_litellm.ps1"
```

## 12.1 改供应商 / 改模型速查表

如果你以后想改配置，优先看这张表：

| 你想改什么 | 去哪里改 | 当前本机地址 | 改什么 |
| --- | --- | --- | --- |
| 把某个 Claude 别名换到别的真实模型 | LiteLLM 模型映射文件 | `C:\Users\prohibit\.claude\litellm.config.yaml` | 改 `model_list` 里对应条目的 `model` |
| 把整套下游从 NVIDIA NIM 换成别的供应商 | LiteLLM 模型映射文件 + 初始化脚本 | `C:\Users\prohibit\.claude\litellm.config.yaml` 和 `C:\Users\prohibit\.claude\nim-bridge\enable_nim_global.ps1` | 改模型前缀、`api_base` 来源、供应商 API Key 变量 |
| 切换 Claude 当前默认模型 | 模型切换脚本 | `C:\Users\prohibit\.claude\nim-bridge\switch-claude-model.ps1` | 改或传入 `ANTHROPIC_MODEL` |
| 重启本机 LiteLLM 网关 | 启停脚本 | `C:\Users\prohibit\.claude\nim-bridge\stop_litellm.ps1` 和 `C:\Users\prohibit\.claude\nim-bridge\start_litellm.ps1` | 停止并重新启动网关 |
| 验证网关当前能不能真的调用模型 | 验证脚本 | `C:\Users\prohibit\.claude\nim-bridge\verify_gateway.ps1` | 发一条真实测试请求 |

### 最常改的地方

#### 情况 1：只是想换模型，不换供应商

这种情况通常只改：

- `C:\Users\prohibit\.claude\litellm.config.yaml`

比如把：

```yaml
- model_name: sonnet-glm5
  litellm_params:
    model: nvidia_nim/z-ai/glm5
```

改成别的同供应商模型：

```yaml
- model_name: sonnet-glm5
  litellm_params:
    model: nvidia_nim/别的模型名
```

改完后执行：

```powershell
powershell -ExecutionPolicy Bypass -File "C:\Users\prohibit\.claude\nim-bridge\stop_litellm.ps1"
powershell -ExecutionPolicy Bypass -File "C:\Users\prohibit\.claude\nim-bridge\start_litellm.ps1"
```

#### 情况 2：想换供应商，但还保留 Claude 这套入口方式

这种情况通常要改两处：

- `C:\Users\prohibit\.claude\litellm.config.yaml`
- `C:\Users\prohibit\.claude\nim-bridge\enable_nim_global.ps1`

因为现在这套写的是 NVIDIA NIM：

- 环境变量名：`NVIDIA_NIM_API_KEY`
- 环境变量名：`NVIDIA_NIM_API_BASE`
- 默认地址：`https://integrate.api.nvidia.com/v1`

如果你换成别的供应商，通常需要：

1. 把 `litellm.config.yaml` 里的：
   - `model: nvidia_nim/...`
   - `api_key: os.environ/NVIDIA_NIM_API_KEY`
   - `api_base: os.environ/NVIDIA_NIM_API_BASE`

   改成新的供应商写法

2. 把 `enable_nim_global.ps1` 里写入用户环境变量的部分改掉，让它写新的供应商变量

3. 保留：
   - `ANTHROPIC_BASE_URL=http://127.0.0.1:4000`
   - `ANTHROPIC_API_KEY=<LiteLLM master key>`

也就是说，只要你还想继续通过 Claude Code 走本机 LiteLLM，这两个一般不用改。

#### 情况 3：只是想切当前默认模型

这种情况不用改映射文件，直接运行：

```powershell
powershell -ExecutionPolicy Bypass -File "C:\Users\prohibit\.claude\nim-bridge\switch-claude-model.ps1" -ModelName "glm5" -RestartVSCode
```

前提是 `glm5` 已经存在于：

- `C:\Users\prohibit\.claude\litellm.config.yaml`

### 当前这台机器上的关键本机地址

下面这些就是你现在这套方案实际用到的本机地址：

#### 文件地址

- LiteLLM 模型映射文件：`C:\Users\prohibit\.claude\litellm.config.yaml`
- 初始化脚本：`C:\Users\prohibit\.claude\nim-bridge\enable_nim_global.ps1`
- 停止网关：`C:\Users\prohibit\.claude\nim-bridge\stop_litellm.ps1`
- 启动网关：`C:\Users\prohibit\.claude\nim-bridge\start_litellm.ps1`
- 切换模型：`C:\Users\prohibit\.claude\nim-bridge\switch-claude-model.ps1`
- 验证网关：`C:\Users\prohibit\.claude\nim-bridge\verify_gateway.ps1`
- 带网关启动 VSCode：`C:\Users\prohibit\.claude\nim-bridge\start-vscode-with-nim.ps1`
- 回滚环境变量：`C:\Users\prohibit\.claude\nim-bridge\disable_nim_global.ps1`
- 网关日志目录：`C:\Users\prohibit\.claude\nim-bridge\logs`
- 网关标准输出日志：`C:\Users\prohibit\.claude\nim-bridge\logs\litellm.stdout.log`
- 网关错误日志：`C:\Users\prohibit\.claude\nim-bridge\logs\litellm.stderr.log`
- 环境变量备份目录：`C:\Users\prohibit\.claude\nim-bridge\backups`

#### 本机网络地址

- LiteLLM 网关地址：`http://127.0.0.1:4000`
- 模型列表接口：`http://127.0.0.1:4000/v1/models`
- 对话接口：`http://127.0.0.1:4000/v1/chat/completions`

#### 当前下游供应商地址

- NVIDIA NIM API Base：`https://integrate.api.nvidia.com/v1`

## 13. 常见问题

### 一直转圈，不出回复

常见原因：

- `sonnet / opus / haiku` 相关别名没有映射
- LiteLLM 没启动
- Claude 进程没吃到新的环境变量

处理方式：

- 先查 `/v1/models`
- 再重启 LiteLLM
- 再彻底关闭并重开 Claude / VSCode

### 401 或鉴权失败

常见原因：

- `ANTHROPIC_API_KEY` 和 `LITELLM_MASTER_KEY` 不一致

处理方式：

- 重新执行 `enable_nim_global.ps1`
- 或手动统一这两个值后重开终端

### 模型改了但列表没更新

常见原因：

- 改完 `litellm.config.yaml` 没重启 LiteLLM

处理方式：

- 执行 `stop_litellm.ps1`
- 再执行 `start_litellm.ps1`
- 再查一次 `/v1/models`

### 下游模型报参数不支持

常见原因：

- Claude 侧传了某些下游不认识的参数

处理方式：

- 确保 `litellm.config.yaml` 中保留：

```yaml
litellm_settings:
  drop_params: true
```

## 14. 安全建议

- 不要把 `NVIDIA_NIM_API_KEY` 直接写死在配置文件里
- 保持 `api_key: os.environ/NVIDIA_NIM_API_KEY`
- 如果密钥已经泄露，先去提供方后台轮换，再重新执行初始化脚本
- 不要把包含真实密钥的截图、日志、聊天记录随便外发

## 15. 这份 README 基于什么整理

这份文档不是凭空猜的，而是从一套已经在本机跑起来的实际配置整理出来的。对应线索包括：

- 用户级环境变量：`ANTHROPIC_BASE_URL`、`ANTHROPIC_MODEL`、`LITELLM_MASTER_KEY`
- LiteLLM 运行进程
- `http://127.0.0.1:4000/v1/models` 的真实返回
- 原始配置文件中的模型映射
- 原始 PowerShell 脚本中的启停、切换和验证逻辑

如果你要给别人复刻，建议优先照本文的通用模板部署，而不是直接复制某个用户目录下写死用户名的路径。
