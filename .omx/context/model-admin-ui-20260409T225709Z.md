# Context Snapshot

task statement:
- 为当前 Claude Code / LiteLLM / NVIDIA NIM 接入体系做一个可视化操作端，方便管理接入的模型和英伟达账号，不再手改文件。

desired outcome:
- 形成清晰、可执行的产品需求说明，之后进入 autopilot workflow 做实现。

stated solution:
- 用户明确要求先 deep-interview，询问清楚需求，再执行 workflow。

probable intent hypothesis:
- 用户希望把当前依赖 `litellm.config.yaml`、PowerShell 脚本和环境变量的模型/账号管理方式，改造成一个更直观、更不容易出错的 GUI/可视化管理端。

known facts/evidence:
- 当前工作区 `D:\claudecode_project` 只有 `.omx` 和一个文档文件 `底层.md`。
- 已存在一套 Claude Code -> LiteLLM -> NVIDIA NIM 的本机接入方案。
- 当前已把 LiteLLM 改造成三账号池结构，关键配置位于：
  - `C:\Users\prohibit\.claude\litellm.config.yaml`
  - `C:\Users\prohibit\.claude\nim-bridge\enable_nim_global.ps1`
- 用户明确提到要使用 `$autopilot`，但在执行前要求先 deep-interview。

constraints:
- 必须先做 deep-interview，不直接进入实现。
- 交互语言使用中文。
- 问题一次只问一个，尽量高杠杆。

unknowns/open questions:
- 用户想管理的对象范围有哪些：模型映射、账号池、默认模型、供应商、环境变量、网关启停、日志、验证等。
- 这个可视化端希望是桌面应用、Web 本地面板、还是 VSCode/Claude 内嵌页面。
- 用户最在意的是“配置方便”还是“观测与运维方便”。
- 哪些操作必须可视化，哪些可以仍然保留脚本。
- 是否需要多工作区、多供应商、导入导出、权限控制。

decision-boundary unknowns:
- OMX/Codex 是否可以自行决定技术栈。
- 是否允许直接修改 `C:\Users\prohibit\.claude\...` 下的真实配置文件。
- 是否需要把敏感 key 明文展示、编辑或只做引用/切换。

likely codebase touchpoints:
- `D:\claudecode_project`
- `C:\Users\prohibit\.claude\litellm.config.yaml`
- `C:\Users\prohibit\.claude\nim-bridge\*.ps1`
